{
  "name": "nudge-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "check:relay-container": "scripts/check-relay-container.sh",
    "relay:start": "npm --workspace @nudge/relay run start"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@xterm/xterm": "^6.0.0"
  }
}
